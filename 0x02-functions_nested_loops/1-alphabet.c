#include <stdio.h>
/**
 * main - Empty point
 *
 * Return: Always 0 (Success)
 */
void print_alphabet(void);
int main(void)
{
	int alpha;
       int alpha = print_alphabet(alpha = 'a'; alpha <= 'z'; alpha++);

	putchar("%d! = %d", alpha);
	putchar("\n");
	return (0);
}

